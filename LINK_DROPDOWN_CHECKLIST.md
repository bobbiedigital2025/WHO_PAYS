# Link & Dropdown Verification Checklist

- [x] Quick link `Members` points to `#members` and scrolls correctly.
- [x] Quick link `Add Transaction` points to `#transactions-form` and scrolls correctly.
- [x] Quick link `Filters` points to `#filters` and scrolls correctly.
- [x] Quick link `Summary` points to `#summary-section` and scrolls correctly.
- [x] Quick link `History` points to `#transactions-table` and scrolls correctly.
- [x] Quick link `MCP` points to `#mcp-tools` and scrolls correctly.
- [x] Dropdown `Person` in transaction form populates from added members.
- [x] Dropdown `Type` includes `Income` and `Expense` and switches correctly.
- [x] Dropdown `Filter Person` includes `All` + all members and filters correctly.
- [x] Dropdown `Filter Type` includes `All`, `Income`, and `Expense` and filters correctly.
- [x] Month picker filter narrows transactions to selected month.
